export const Datatypes: Datatypes = {
    STRING: 'string',
    BOOLEAN: 'boolean',
    ARRAY: 'array',
    ENUM: 'enum',
    OBJECT: 'object',
    INTEGER: 'int',
    DOUBLE: 'double',
    DATE_TIME: 'datetime',
    DATE: 'date',
    TIME: 'time'
};

interface Datatypes {
    STRING: string;
    BOOLEAN: string;
    ARRAY: string;
    ENUM: string;
    OBJECT: string;
    INTEGER: string;
    DOUBLE: string;
    DATE_TIME: string;
    DATE: string;
    TIME: string;
};