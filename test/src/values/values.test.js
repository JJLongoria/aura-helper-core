const Values = require('../../../src/values');

describe('Testing ./src/utils/httpConnection.js', () => {
    test('Testing getRequest()', () => {
        expect(Values.ApexNodeTypes.ANNOTATION).toEqual(Values.ApexNodeTypes.ANNOTATION);
    });
});